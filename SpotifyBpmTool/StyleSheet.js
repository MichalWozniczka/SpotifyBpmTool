import { StyleSheet } from "react-native";

export const styles = StyleSheet.create ({
    libraryItem: {
        borderRadius: 10, 
        padding: 10, 
        margin: 3, 
        flexDirection: "row", 
        alignItems: "center",
        backgroundColor: "#2e2e2e",
    },
    header: {
        color: "white", 
        fontSize: 25, 
        fontWeight: "bold"
    }
});